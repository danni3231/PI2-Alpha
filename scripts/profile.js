const btnData = document.querySelector('.profileMain__btnData');
const btnRecord = document.querySelector('.profileMain__btnRecord');

btnData.addEventListener('click', ()=>{
    location.href=  'misDatos.html';
});

console.log(btnData);